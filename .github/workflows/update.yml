name: Update Market Data

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

# This tells GitHub to use the newer Node.js 24 engine
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  refresh-data:
    runs-on: ubuntu-latest
    steps:
      # Use @v5 or @v6 for the latest Node support
      - name: Checkout repository
        uses: actions/checkout@v4 

      # Use @v5 for the latest Python setup support
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build fresh dataset
        run: python data_engine.py

      - name: Commit and push if data changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add processed_data.csv
          if git diff --staged --quiet; then
            echo "No dataset changes detected."
          else
            git commit -m "chore(data): daily market data refresh"
            git push
          fi